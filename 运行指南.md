# 南竹竹制品管理系统 - 运行指南

## 快速开始

### 方式一：一键启动（推荐）

1. 打开 `d:\code\web\` 目录
2. 双击运行 `快速启动.bat`
3. 选择启动方式

### 方式二：手动启动

#### 启动后端
```bash
cd d:\code\web\backend

# 设置环境变量
set PATH=D:\miniconda3;D:\miniconda3\Scripts;D:\miniconda3\Library\bin;%PATH%

# 安装依赖（仅首次）
pip install -r requirements.txt

# 启动服务
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

#### 启动前端
直接在浏览器中打开：
- 官网：`d:\code\web\frontend\public\index.html`
- 后台：`d:\code\web\frontend\public\admin.html`

## 访问地址

| 服务 | 地址 |
|------|------|
| 后端API | http://localhost:8000 |
| API文档 | http://localhost:8000/docs |
| 企业官网 | frontend/public/index.html |
| 管理后台 | frontend/public/admin.html |

## 默认账号

| 用户名 | 密码 |
|--------|------|
| admin | admin123 |

## 项目结构

```
d:\code\web\
├── 快速启动.bat           # 快速启动脚本
├── setup-python.bat       # Python环境设置
├── setup-git.bat          # Git环境设置
├── git-commit.bat         # Git提交脚本
├── git-push.bat          # Git推送脚本
│
├── backend/              # 后端服务
│   ├── run.bat          # 后端启动脚本
│   ├── requirements.txt # Python依赖
│   └── app/             # FastAPI应用
│
└── frontend/            # 前端应用
    └── public/          # 静态文件
        ├── index.html   # 企业官网
        └── admin.html   # 管理后台
```

## 依赖安装说明

依赖安装需要编译一些库（如pydantic-core），首次安装可能需要5-10分钟，请耐心等待。

如果安装失败，可以尝试：
```bash
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 常见问题

### Q: 找不到Python命令
A: 运行 `setup-python.bat` 设置环境变量

### Q: 依赖安装失败
A: 检查网络连接，或使用国内镜像源

### Q: 后端启动后无法访问
A: 确认端口8000未被占用，防火墙已放行

### Q: 前端页面无法连接后端
A: 确认后端服务已启动，API_BASE_URL配置正确

## 技术支持

如遇问题，请检查：
1. Python版本 >= 3.8
2. 所有依赖已正确安装
3. 端口8000可访问
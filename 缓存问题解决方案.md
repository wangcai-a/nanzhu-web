# 浏览器缓存问题解决方案

## 问题描述
每次修改代码（HTML、CSS、JS）后，浏览器显示的是旧版本，需要手动清理缓存。

## 解决方案（多重保障）

### ✅ 方案一：服务器端禁用缓存（已配置）
服务器已配置所有响应禁用缓存，浏览器会自动获取最新版本。

**优势：**
- 无需手动操作
- 每次刷新都会获取最新版本
- 最推荐的方案

**验证方法：**
1. 打开浏览器开发者工具 (F12)
2. 切换到 Network 标签
3. 访问任意页面
4. 查看任意文件的 Response Headers，应包含：
   ```
   Cache-Control: no-cache, no-store, must-revalidate
   Pragma: no-cache
   Expires: 0
   ```

### ✅ 方案二：版本号查询参数（已配置）
所有 HTML 文件中的 CSS/JS 引用已添加版本号参数。

**优势：**
- 即使服务器缓存控制失效，浏览器也会因 URL 变化而获取新文件
- 双重保险

**劣势：**
- 每次修改代码后需要手动更新版本号

**如何更新版本号：**
```powershell
# 方法1：运行自动更新脚本
.\update-versions.ps1

# 方法2：手动修改 HTML 文件中的版本号
# 将所有 ?v=1.0.1 改为新的版本号，如 ?v=1.0.2
```

### ✅ 方案三：浏览器强制刷新（推荐备用方案）
即使服务器禁用缓存，有时浏览器仍可能使用本地缓存。

**操作步骤：**
1. **Windows/Linux：** 按 `Ctrl + Shift + R`
2. **Mac：** 按 `Cmd + Shift + R`
3. 或者按住 `Ctrl` (或 `Cmd`) 并点击刷新按钮

**或在开发者工具中：**
1. 打开开发者工具 (F12)
2. 在 Network 标签中，勾选 "Disable cache"
3. 保持开发者工具打开，刷新页面

### ✅ 方案四：清理浏览器缓存（终极方案）
如果以上方案都无效，使用清理脚本。

```powershell
# 运行清理脚本
.\clear-cache.ps1
```

**清理脚本功能：**
- 自动清理 Trae 内嵌浏览器的所有缓存
- 显示清理统计信息
- 提供使用提示

## 推荐的日常工作流程

### 修改 HTML、CSS 或 JS 文件后：
1. **首选：** 直接在浏览器中按 `Ctrl + Shift + R` 强制刷新
2. **如果不行：** 运行 `.\update-versions.ps1` 更新版本号，然后刷新
3. **如果还不行：** 运行 `.\clear-cache.ps1` 清理浏览器缓存，然后刷新

### 修改 Python 后端代码后：
1. 重启服务器：
   ```powershell
   cd d:\code\web
   .\start.bat
   ```
2. 在浏览器中按 `Ctrl + Shift + R` 刷新页面

## 文件说明

- `update-versions.ps1` - 自动更新所有 HTML 文件中的版本号
- `clear-cache.ps1` - 清理浏览器缓存的脚本
- `backend\app\main.py` - 服务器端缓存控制配置

## 技术实现细节

### 服务器端缓存控制
在 `backend\app\main.py` 中：
```python
class NoCacheMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request: Request, call_next):
        response = await call_next(request)
        response.headers["Cache-Control"] = "no-cache, no-store, must-revalidate"
        response.headers["Pragma"] = "no-cache"
        response.headers["Expires"] = "0"
        return response

app.add_middleware(NoCacheMiddleware)
```

### HTML 版本号引用
```html
<link rel="stylesheet" href="/assets/styles.css?v=1.0.2">
<script src="/assets/script.js?v=1.0.2"></script>
```

## 常见问题

**Q: 为什么服务器已禁用缓存，但浏览器仍显示旧版本？**
A: 可能是浏览器扩展、代理服务器或 CDN 缓存。尝试方案三和方案四。

**Q: 需要每次都运行 update-versions.ps1 吗？**
A: 不需要。只有当服务器端缓存控制失效，或者修改了 CSS/JS 文件后才需要。

**Q: 能否自动化版本号更新？**
A: 可以，但考虑到性能开销和复杂性，目前采用手动更新方式。大多数情况下，服务器端缓存控制已足够。

**Q: 生产环境也需要禁用缓存吗？**
A: 生产环境通常需要启用缓存以提升性能。建议：
- 开发环境：禁用缓存
- 生产环境：使用版本号+长缓存周期（如1年），更新时更改版本号
